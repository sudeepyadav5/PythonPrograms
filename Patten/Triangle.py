"""
*
**
***
****
*****

"""
"""n = 5
for row in range(n+1):
    for col in range(row):
        print("*", end='')
    print("")
"""
"""
*****
****
***
**
*
"""

# def print_pattern(rows):
#     pattern = ""  # Initialize an empty string to store the pattern
#     for i in range(rows, 0, -1):  # Iterate from the number of rows to 1 (inclusive)
#         for j in range(i):
#             pattern += "*"
#         pattern += "\n"  # Add a newline character to move to the next line
#
#     # Print the pattern
#     print(pattern)
#
# # Example usage
# print_pattern(7)
"""
rows = 5
pattern = ""  # Initialize an empty string to store the pattern
for i in range(rows, 0, -1):  # Iterate from the number of rows to 1 (inclusive)
    for j in range(i):
        pattern += "*"
    pattern += "\n"  # Add a newline character to move to the next line

# Print the pattern
print(pattern)"""

"""rows = 5
pattern = ""  # Initialize an empty string to store the pattern
for i in range(rows, 0, -1):  # Iterate from the number of rows to 1 (inclusive)
    for j in range(i):
        pattern += "*"
    pattern += "\n"  # Add a newline character to move to the next line

# Print the pattern
print(pattern)"""
